$Pr(Y \vert X)$  % 조건부확률, Pr(Y|X)

$X_1, ..., X_p$ % 랜덤샘플 p개

$\in$ % 원소 포함 기호

% 괄호 모음
$\left [ \right ]$ % 대괄호
$\left { \right }$ % 중괄호
$\left ( \right )$ % 소괄호

% taylor expansion
$A(\tilde{s}) \approx A(s) + \nabla A(s)^T(\tilde{s} - s) + \frac{1}{2}(\tilde{s} - s)^T \nabla^2 A(s)(\tilde{s} - s)$

% 등호 위에 def
\stackrel{\text{def}}{=}

% align 환경에서 수식 정렬
&=
&:=

%
begin{theorem}

end{theorem}

begin{proof}

end{proof}

